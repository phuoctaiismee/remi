import Image from "next/image";

export default function Home() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-zinc-50 font-sans dark:bg-black">
      <main className="flex min-h-screen border w-full max-w-3xl mx-auto text-center flex-col items-center justify-between py-32 px-16 bg-white dark:bg-black">
        Hello mấy cưng
      </main>
    </div>
  );
}
